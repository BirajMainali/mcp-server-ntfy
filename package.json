{
  "name": "mcp-server-ntfy",
  "version": "1.0.0",
  "description": "ntfy MCP server—outbound alerts so the operator can leave the session; signal outcomes, blockers, and requested checkpoints only",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-server-ntfy": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
